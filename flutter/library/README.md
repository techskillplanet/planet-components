# TechSkillPlanet Basic Controls for Flutter

Flutter implementation of the TechSkillPlanet `Tsp` basic controls.

Library code lives in `lib/`; the runnable visual sample lives in `example/` and depends on the local package.

```dart
import 'package:tech_skill_planet_components/tech_skill_planet_components.dart';
```
