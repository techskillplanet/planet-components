# Android（兼容路径）

本目录为 **`android/`** 的符号链接，供仍使用旧路径 `android-view` 的 IDE / 脚本兼容。

请优先使用：

```bash
cd android
./gradlew :samples:assembleDebug
```

Maven 坐标：`io.github.techskillplanet:planet-components-android`
