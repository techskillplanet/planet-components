# Copy to tools/publish-npm-web.env (gitignored). Never commit real tokens.
# cp tools/publish-npm-web.env.example tools/publish-npm-web.env
#
# Create an npm Automation token (or Granular token with Bypass 2FA) at:
# https://www.npmjs.com/settings/~/tokens
# Rotate anytime if leaked.

NPM_TOKEN=
