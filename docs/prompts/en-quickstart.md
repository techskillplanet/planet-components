# Prompt cheat sheet (English)

Use with skill **`ai-build-with-planet`**. Default: React web + `@techskillplanet/planet-components-react@0.2.0` + Sky theme.

## Bootstrap (paste first)

```text
Build UI with TechSkillPlanet Planet Components (@techskillplanet/planet-components-react@0.2.0).
Rules:
1) Use Tsp* components only for buttons, bars, cards, inputs — no custom div redesign.
2) Import styles.css once.
3) Pass theme={starPlanetThemes.sky} to every control.
4) Explain in plain language how I run the preview.
5) Follow skill ai-build-with-planet.
If no app exists, scaffold Vite + React minimally first.
```

## Examples

```text
Create a parent home screen: top bar "This week", three cards (check-in, points, redeem), primary footer button "Continue".
```

```text
Create a login screen with phone, code, primary Login button, and a text link for sign-up.
```

```text
Switch theme to night. Keep the same layout.
```
